# Adapty Revenue Export Scripts

Набор специализированных Python-скриптов для выгрузки и обработки данных о выручке ( Revenue ) из **Adapty Client API**.  
Каждый скрипт отвечает за свой тип выборки — все длительности, только Monthly/Annual, по App Store / Play Store, по странам, а также по отдельным категориям подписок.

Скрипты используются как часть аналитического пайплайна для:
- расчёта LTV по странам и витринам (P0…P11),
- анализа помесячных и годовых подписок,
- построения когорных таблиц,
- прогноза выручки

---

## 🚀 Структура проекта

| Скрипт | Описание |
|-------|----------|
| **revenue_all.py** | Базовая выгрузка всех типов подписок и всех длительностей из Adapty. |
| **revenue_all_monthly.py** | Фильтрация данных только по Monthly-подпискам. |
| **revenue_all_yearly.py** | Выгрузка Annual (годовых) подписок + forecast логика. |
| **revenue_app_store.py** | Выручка только по App Store (iOS). |
| **revenue_app_store_monthly.py** | App Store + Monthly. |
| **revenue_app_store_yearly.py** | App Store + Annual. |
| **revenue_play_store.py** | Выручка только по Play Store (Android). |
| **revenue_play_store_monthly.py** | Play Store + Monthly. |
| **revenue_play_store_yearly.py** | Play Store + Annual. |

Каждый файл содержит:
- конфигурацию диапазона дат,
- набор стран,
- параметры фильтрации подписок,
- HTTP-клиент с ретраями,
- обработку когорных значений,
- сборку финальных таблиц в CSV.

---

## ⚙️ Как развернуть проект

1. **Клонировать репозиторий**

    git clone https://github.com/nicksetrakov/adapty-revenue-ltv.git
    cd adapty-revenue-ltv

2. **Создать виртуальное окружение**

    # Windows
    python -m venv venv
    venv\Scripts\activate

    # macOS / Linux
    python -m venv venv
    source venv/bin/activate

3. **Установить зависимости**

    pip install -r requirements.txt

5. **Запустить нужный скрипт**

    python revenue_all.py

📌 Все CSV-файлы будут сохранены в директории reports репозитория.
